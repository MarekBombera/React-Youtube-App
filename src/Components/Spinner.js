import { AiOutlineLoading3Quarters } from "react-icons/ai";

const Spinner = () => {
	return (
		<div className="loading-icon">
			<AiOutlineLoading3Quarters className="spinner" icon="spinner" />
		</div>
	);
};

export default Spinner;
